server:
  port: 8084
  servlet:
    context-path: /provider
spring:
  application:
    name: provider-service
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://47.114.33.113/register-eureka/eureka/